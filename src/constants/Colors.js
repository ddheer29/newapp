import {Dimensions} from 'react-native';

export const Colors = {
  primary: 'rgb(105, 63, 242)',
};

export const screenWidth = Dimensions.get('window').width;
export const screenHeight = Dimensions.get('window').height;
